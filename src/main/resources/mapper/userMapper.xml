<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.restfulwebservice.user.UserDao">
    <select id="totalRecord" resultType="int">

    </select>
    <select id="findList" resultType="com.example.restfulwebservice.user.User">

    </select>
    <select id="findOne" parameterType="int" resultType="com.example.restfulwebservice.user.User">

    </select>
    <insert id="save" parameterType="com.example.restfulwebservice.user.User">

    </insert>
    <update id="update" parameterType="com.example.restfulwebservice.user.User">

    </update>
    <update id="delete" parameterType="int">

    </update>
</mapper>